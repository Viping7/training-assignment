import { MarkCustomJokeDirective } from './mark-custom-joke.directive';

describe('MarkCustomJokeDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkCustomJokeDirective();
    expect(directive).toBeTruthy();
  });
});
